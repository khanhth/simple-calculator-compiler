## TODOS
- Lexer is mock now. Implement a real lexer.
- In [https://share.google/aimode/MVU7ycp6rbxdSjM5e](this) thread explore:
1. How to update the code to track and clean up temporary registers so you don't run out of them (Register Allocation).
2. How a Symbol Table tracks scope blocks (like local vs. global variables) inside the AddressTable.getOffset() step.
3. How to handle unassigned variables or type mismatches before the code generation step runs.

